name: datajobs
version: '1.0.0'
profile: ohio_transforming

model-paths: ["models"]
macro-paths: ["macros"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  datajobs:
    staging:
      +materialized: incremental
      +schema: staging
    intermediate:
      +materialized: incremental
      +schema: intermediate
    marts:
      +materialized: incremental
      +schema: marts
